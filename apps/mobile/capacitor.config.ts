import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.constware.app',
  appName: 'Constware',
  webDir: '../../apps/web/out'
};

export default config;
