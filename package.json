{
  "name": "constware-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@10.8.2",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "postinstall": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate"
  },
  "devDependencies": {
    "turbo": "^2.0.4",
    "typescript": "^5.4.5",
    "prisma": "^5.14.0",
    "eslint": "^8"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "overrides": {
    "glob": "^13.0.0",
    "rimraf": "^6.0.0",
    "tar": "^7.0.0"
  }
}
